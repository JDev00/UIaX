package uiax.colors;

import uia.core.rendering.color.Color;

/**
 * Collection of Darcula theme colors.
 */

public final class DarculaColorCollection {
    public static final Color BLUE = Color.createColor(41, 41, 204);
    public static final Color DARK_GRAY = Color.createColor(40, 42, 54);
    public static final Color LIGHT_GRAY = Color.createColor(231);

    private DarculaColorCollection() {
    }
}
